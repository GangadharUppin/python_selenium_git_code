pipeline {
    agent { label 'ec2' }  // Replace with the actual label of your EC2 agent

    stages {
        stage('Verify Node') {
            steps {
                sh 'echo "Running on EC2"'
                sh 'uname -a'
                sh 'whoami'
                sh 'which git'
            }
        }
    }
}
