pipeline {
    agent { label 'ec2' }  // must match the label of your EC2 node

    stages {
        stage('Git Clone') {
            steps {
                git url: 'https://github.com/GangadharUppin/python_selenium_git_code.git'
            }
        }
    }
}
