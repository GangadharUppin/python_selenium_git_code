pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                git(
                    url: 'https://github.com/GangadharUppin/python_selenium_git_code.git',
                    branch: 'master',
//                     credentialsId: 'your-credential-id' // if private repo
                )
            }
        }
    }
}
