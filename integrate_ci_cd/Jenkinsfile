pipeline {

    agent { label 'linux' }   // Use your EC2 agent label

    stages {
        stage("Now clone the repo") {
            steps {
                sh 'rm -rf python_selenium_git_code && git clone https://github.com/GangadharUppin/python_selenium_git_code.git'
            }
        }
    }

    post {
        always {
            echo 'Cleaning up...'
        }
        success {
            echo 'Build and tests successful.'
        }
        failure {
            echo 'Build or tests failed.'
        }
    }
}